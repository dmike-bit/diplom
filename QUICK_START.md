# 🚀 Быстрый запуск Django Блога "Матрица"

## ⚡ Мгновенный запуск

### Вариант 1: Проверенный способ (рекомендуется)
```bash
# 1. Установить зависимости
pip install Django==5.2.8 django-crispy-forms==2.1 crispy-bootstrap5

# 2. Инициализировать БД
python manage.py migrate --settings=myblog.settings_simple

# 3. Создать суперпользователя
python manage.py createsuperuser --settings=myblog.settings_simple

# 4. Запустить сервер
python manage.py runserver 8000 --settings=myblog.settings_simple
```

### Вариант 2: Автоматический скрипт
```bash
python start_local.py
```
Скрипт автоматически выполнит все настройки и запустит сервер.

## 🌐 Доступ к сайту

После запуска откройте в браузере:

- **Главная страница**: http://127.0.0.1:8000
- **Админ панель**: http://127.0.0.1:8000/admin

## 🔧 Функции в локальном режиме

✅ **Полная функциональность блога:**
- Создание и редактирование постов
- Система комментариев
- Регистрация и авторизация пользователей
- Профили пользователей
- Поиск по постам
- Категории и теги

❌ **Ограничения локального режима:**
- Нет REST API
- Нет WebSocket уведомлений
- Нет Redis кэширования
- Используется SQLite вместо PostgreSQL

## 🐳 Полная функциональность (Docker)

Для получения всех возможностей используйте Docker:

```bash
docker-compose up -d --build
```

## 📚 Документация

Полная документация доступна в файле `README.md`.