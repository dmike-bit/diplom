# Матрица Блог - Современный Django Проект

Современный блог на Django с поддержкой API, WebSocket, PostgreSQL, Redis и Docker.

## 🚀 Возможности

### Основной функционал
- **Блог система** с постами, комментариями, категориями
- **Система пользователей** с профилями и ролями
- **Real-time уведомления** через WebSocket
- **REST API** для мобильных приложений и SPA
- **JWT аутентификация**
- **Кэширование** с Redis
- **Система уведомлений** с email
- **Админ панель** для управления контентом

### Современные технологии
- **Docker** контейнеризация
- **Nginx** как reverse proxy
- **PostgreSQL** база данных
- **Redis** для кэширования и сессий
- **Celery** для фоновых задач
- **Django REST Framework** для API
- **Channels** для WebSocket
- **Health checks** и мониторинг

### Безопасность
- **CSRF защита**
- **SQL injection защита**
- **XSS защита**
- **Rate limiting** для API
- **Axes** для защиты от брутфорса
- **HTTPS** готовность
- **Безопасные заголовки**

## 📋 Требования

- Docker и Docker Compose
- Python 3.11+
- 2GB+ RAM
- 5GB+ свободного места

## 🛠️ Быстрый старт

### 🚀 Вариант 1: Docker (рекомендуется для production)

#### 1. Клонирование и настройка

```bash
git clone <repository-url>
cd myblog
```

#### 2. Настройка переменных окружения

```bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

#### 3. Запуск через Docker

```bash
# Сборка и запуск всех сервисов
docker-compose up -d --build

# Или запуск в фоновом режиме
docker-compose up -d
```

#### 4. Инициализация базы данных

```bash
# Выполнение миграций
docker-compose exec web python manage.py migrate

# Создание суперпользователя
docker-compose exec web python manage.py createsuperuser

# Сбор статических файлов
docker-compose exec web python manage.py collectstatic --noinput

# Загрузка демо данных (опционально)
docker-compose exec web python manage.py create_sample_data
```

#### 5. Проверка работы

- **Веб-сайт**: http://localhost
- **Админка**: http://localhost/admin
- **API документация**: http://localhost/api/docs/
- **Health check**: http://localhost/health/
- **Мониторинг**: http://localhost:3000 (Grafana)

### 🔧 Вариант 2: Локальная разработка (упрощенная)

#### 1. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\\Scripts\\activate  # Windows
```

#### 2. Установка базовых зависимостей

```bash
pip install Django==5.2.8 django-crispy-forms==2.1
```

#### 3. Запуск с упрощенными настройками

```bash
# Установить переменную окружения для локальных настроек
export DJANGO_SETTINGS_MODULE=myblog.settings_local

# Или запустить с указанием настроек
python manage.py runserver --settings=myblog.settings_local
```

#### 4. Инициализация базы данных

```bash
python manage.py migrate --settings=myblog.settings_local
python manage.py createsuperuser --settings=myblog.settings_local
```

#### 5. Запуск сервера

```bash
python manage.py runserver --settings=myblog.settings_local
```

### 🛠️ Вариант 3: Полная локальная установка

#### 1. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\\Scripts\\activate  # Windows
```

#### 2. Установка всех зависимостей

```bash
pip install -r requirements.txt
```

#### 3. Настройка базы данных (PostgreSQL опционально)

```bash
# Для полной функциональности нужен PostgreSQL и Redis
# Установите PostgreSQL и Redis локально
# Или используйте Docker для БД:
docker run -d --name postgres -e POSTGRES_PASSWORD=postgres123 -e POSTGRES_DB=myblog -p 5432:5432 postgres:15
docker run -d --name redis -p 6379:6379 redis:7-alpine
```

#### 4. Настройка переменных окружения

```bash
cp .env.example .env
# Отредактируйте .env файл
```

#### 5. Запуск приложения

```bash
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

## 📁 Структура проекта

```
myblog/
├── blog/                    # Основное приложение
│   ├── api/                # REST API
│   ├── migrations/         # Миграции БД
│   ├── static/            # Статические файлы
│   ├── templates/         # HTML шаблоны
│   ├── models.py         # Модели данных
│   ├── views.py          # Представления
│   ├── urls.py           # URL маршруты
│   ├── consumers.py      # WebSocket consumers
│   └── routing.py        # WebSocket routing
├── myblog/               # Конфигурация проекта
│   ├── settings.py       # Настройки Django
│   ├── urls.py           # Главные URL
│   ├── wsgi.py           # WSGI конфигурация
│   └── asgi.py           # ASGI конфигурация
├── nginx/                # Конфигурация Nginx
├── monitoring/           # Мониторинг и метрики
├── docker-compose.yml    # Docker Compose конфигурация
├── Dockerfile           # Docker образ
└── requirements.txt     # Python зависимости
```

## 🔌 API Документация

### Основные endpoints

- `GET /api/posts/` - Список постов
- `POST /api/posts/` - Создание поста (аутентификация)
- `GET /api/posts/{id}/` - Детали поста
- `POST /api/posts/{id}/like/` - Лайк поста
- `POST /api/auth/register/` - Регистрация
- `POST /api/auth/login/` - Вход
- `GET /api/profile/` - Профиль пользователя

### Аутентификация

API использует JWT токены:

```bash
# Получение токена
POST /api/auth/login/
{
  "username": "username",
  "password": "password"
}

# Использование токена
Authorization: Bearer <access_token>
```

## 🐳 Docker сервисы

| Сервис | Порт | Описание |
|--------|------|----------|
| web | 8000 | Django приложение |
| db | 5432 | PostgreSQL |
| redis | 6379 | Redis кэш |
| nginx | 80/443 | Nginx proxy |
| celery | - | Celery worker |
| celery-beat | - | Celery scheduler |
| prometheus | 9090 | Мониторинг |
| grafana | 3000 | Дашборды |

## 📊 Мониторинг

### Health Checks
- `GET /health/` - Проверка состояния системы
- `GET /stats/` - Статистика системы

### Метрики
- **Prometheus** собирает метрики
- **Grafana** для визуализации
- **Логи** в `/logs/`

## 🚀 Развертывание в продакшн

### 1. Настройка SSL

```bash
# Получите SSL сертификаты
# Раскомментируйте HTTPS блок в nginx/sites-available/myblog
# Добавьте сертификаты в nginx/ssl/
```

### 2. Настройка переменных окружения

```bash
# В .env файле установите:
DEBUG=False
SECRET_KEY=ваш-секретный-ключ
ALLOWED_HOSTS=ваш-домен.com
```

### 3. Оптимизация производительности

```bash
# Увеличьте количество воркеров
# Настройте кэширование
# Оптимизируйте базу данных
```

### 4. Настройка резервного копирования

```bash
# Настройте автоматические бэкапы БД
docker-compose exec db pg_dump -U postgres myblog > backup.sql
```

## 🔧 Разработка

### Добавление нового функционала

1. Создайте модели в `blog/models.py`
2. Создайте миграции: `python manage.py makemigrations`
3. Добавьте API endpoints в `blog/api/`
4. Создайте представления в `blog/views.py`
5. Обновите URL маршруты

### Тестирование

```bash
# Запуск тестов
python manage.py test

# Тестирование API
curl -X GET http://localhost/api/posts/
```

## 🛠️ Полезные команды

```bash
# Django команды
docker-compose exec web python manage.py shell
docker-compose exec web python manage.py migrate
docker-compose exec web python manage.py collectstatic

# Работа с Redis
docker-compose exec redis redis-cli

# Просмотр логов
docker-compose logs -f web
docker-compose logs -f nginx

# Перезапуск сервисов
docker-compose restart web

# Обновление образов
docker-compose pull
docker-compose up -d --force-recreate
```

## 🐛 Решение проблем

### Проблемы с базой данных
```bash
# Сброс БД
docker-compose down -v
docker-compose up -d db
docker-compose exec web python manage.py migrate
```

### Проблемы с статическими файлами
```bash
docker-compose exec web python manage.py collectstatic --noinput --clear
```

### Проблемы с Redis
```bash
# Очистка кэша
docker-compose exec web python manage.py shell -c "from django.core.cache import cache; cache.clear()"
```

## 📈 Масштабирование

### Горизонтальное масштабирование
```bash
# Добавление дополнительных воркеров
docker-compose up -d --scale web=3
```

### Вертикальное масштабирование
- Увеличьте ресурсы для `db` и `redis`
- Настройте connection pooling в Django
- Оптимизируйте настройки кэширования

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Создайте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь что все сервисы запущены: `docker-compose ps`
3. Проверьте health check: `curl http://localhost/health/`
4. Создайте issue в репозитории

---

**Матрица Блог** - современное решение для создания блогов с полным набором необходимого функционала.