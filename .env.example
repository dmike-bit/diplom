# ============================================================================
# ПРИМЕРЫ БЕЗОПАСНЫХ ПЕРЕМЕННЫХ ОКРУЖЕНИЯ
# Скопируйте этот файл в .env и заполните РЕАЛЬНЫМИ значениями!
# НЕ ДОБАВЛЯЙТЕ .env в Git!
# ============================================================================

# 🚨 КРИТИЧЕСКИ ВАЖНО: Django секретный ключ (ОБЯЗАТЕЛЬНО!)
# Сгенерировать новый: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-super-secret-django-key-here

# 🚨 КРИТИЧЕСКИ ВАЖНО: База данных (ОБЯЗАТЕЛЬНО!)
DB_NAME=myblog_db
DB_USER=myblog_user
DB_PASSWORD=super-strong-password-here
DB_HOST=localhost
DB_PORT=5432

# 🚨 КРИТИЧЕСКИ ВАЖНО: Redis (ОБЯЗАТЕЛЬНО!)
REDIS_URL=redis://localhost:6379/0

# 🚨 КРИТИЧЕСКИ ВАЖНО: Email конфигурация (ОБЯЗАТЕЛЬНО!)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# 🚨 КРИТИЧЕСКИ ВАЖНО: Celery (ОБЯЗАТЕЛЬНО!)
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# 🔧 Настройки приложения
DEBUG=false
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
PRODUCTION_DOMAIN=your-domain.com
SECURE_SSL_REDIRECT=true
SECURE_COOP=true

# 🛡️ Дополнительные настройки безопасности
SECURE_HTTPS_ONLY=true
SECURE_BROWSER_XSS_FILTER=true
SECURE_CONTENT_TYPE_NOSNIFF=true

# 📊 Мониторинг и логирование
DJANGO_LOG_LEVEL=INFO
SENTRY_DSN=your-sentry-dsn-here

# 🗂️ Файловые настройки
USE_LOCAL_CACHE=false
FILE_UPLOAD_MAX_SIZE=5242880

# 🌐 CORS настройки (раскомментируйте если нужно)
# CORS_ALLOW_ALL_ORIGINS=false
# CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com

# 📧 Email отправитель по умолчанию
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# 🗺️ Локализация
LANGUAGE_CODE=ru-ru
TIME_ZONE=Europe/Moscow